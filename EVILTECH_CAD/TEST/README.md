# Test Suite

The `TEST` package contains focused regression coverage for each major EvilTech
CAD subsystem. Tests are designed to validate deterministic, headless behavior
so the project remains buildable and verifiable in non-GUI environments.
