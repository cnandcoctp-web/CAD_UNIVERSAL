# AI Analysis Pipeline

The `AI` package provides a deterministic, headless analysis and recommendation
pipeline for EvilTech CAD. It consumes real project, modeling, geometry, and
constraint data, evaluates manufacturability and design-quality heuristics,
tracks recommendation outcomes, and emits validated recommendations without
introducing external model dependencies or cloud services.
