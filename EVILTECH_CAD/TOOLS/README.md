# Tools

The `TOOLS` package contains reusable geometry and modeling utilities for
inspection, measurement, translation, rotation, scaling, mirroring, sectioning,
and lightweight edge-detail operations such as chamfer and fillet.
