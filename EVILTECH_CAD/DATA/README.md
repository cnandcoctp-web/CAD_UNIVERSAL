# Data Libraries

The `DATA` package contains reusable engineering catalogs and project presets,
including materials, fasteners, bearings, gears, standards, textures, templates,
and example projects. The catalogs are headless and deterministic so they can be
consumed consistently by UI, modeling, IO, and AI workflows.
