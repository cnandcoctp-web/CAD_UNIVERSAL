# Design Context

The `DESIGN_CONTEXT` package stores session-level design state such as
annotations, bookmarks, metadata, feature-tree presentation, history events,
and undo/redo workflows. These modules are intentionally UI-agnostic so they
can support desktop, scripted, and AI-assisted workflows consistently.
