# Utilities

The `UTILS` package provides shared helper infrastructure for configuration,
validation, serialization, caching, timing, structured logging, and decorator
support. These modules are intentionally lightweight so that higher-level
packages can depend on them without pulling in UI, modeling, or AI concerns.
